{
  "name": "corner-gym",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @corner-gym/desktop dev",
    "build": "pnpm -r build",
    "test": "pnpm --filter @corner-gym/engine test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "prettier": "^3.2.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild"]
  }
}
