# Tell prebuild-install to download binaries compiled for Electron, not system Node.
# This ensures better-sqlite3 loads without ABI version mismatch when pnpm install runs.
runtime=electron
target=33.4.11
target_arch=x64
dist_url=https://electronjs.org/headers
